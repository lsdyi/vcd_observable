// get normalized weights for each datapoint
// have a property w for each datapoint object 
export const getWeights = (data, condition, keys) => {
    // const data = creditCard.map((item) => _.pick(item, keys));
    console.log(data)
}
